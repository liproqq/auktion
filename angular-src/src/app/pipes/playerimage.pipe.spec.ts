import { PlayerimagePipe } from './playerimage.pipe';

describe('PlayerimagePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerimagePipe();
    expect(pipe).toBeTruthy();
  });
});
